name: CS-Loyalty-001 context separation

on:
  workflow_dispatch:
  push:
    branches:
      - research/context-separation-loyalty-v1
    paths:
      - "kurukshetra/context_separation/**"
      - ".github/workflows/context-separation-loyalty.yml"

permissions:
  contents: read

jobs:
  seed:
    name: Seed ${{ matrix.seed }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        seed: [0, 1, 2]

    steps:
      - name: Checkout frozen branch
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: kurukshetra/requirements.txt

      - name: Install dependencies
        run: python -m pip install -r kurukshetra/requirements.txt

      - name: Run frozen seed
        working-directory: kurukshetra
        run: |
          set -o pipefail
          python context_separation/experiment.py --seed ${{ matrix.seed }} 2>&1 | tee results/context_separation_seed${{ matrix.seed }}.log

      - name: Upload immutable seed evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cs-loyalty-seed-${{ matrix.seed }}
          path: |
            kurukshetra/results/context_separation_seed${{ matrix.seed }}.json
            kurukshetra/results/context_separation_seed${{ matrix.seed }}.log
          if-no-files-found: warn
          retention-days: 30

  aggregate:
    name: Frozen aggregate verdict
    runs-on: ubuntu-latest
    needs: seed
    if: always()

    steps:
      - name: Checkout frozen branch
        uses: actions/checkout@v6

      - name: Download all seed artifacts
        uses: actions/download-artifact@v5
        with:
          pattern: cs-loyalty-seed-*
          path: artifacts

      - name: Apply preregistered aggregate gate
        run: python kurukshetra/context_separation/aggregate.py --input artifacts --output aggregate

      - name: Publish aggregate evidence
        uses: actions/upload-artifact@v4
        with:
          name: CS-Loyalty-001-aggregate
          path: aggregate/
          retention-days: 30
